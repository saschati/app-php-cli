name: 'Run php-cli'
description: 'Github Action run php-cli with installed composer'
author: 'Olexander Grigorchyk'
branding:
  icon: 'cast'
  color: 'black'
inputs:
  command:
    description: 'The command how need run in action with install composer'
  dir:
    description: 'The directory where your composer.json is located'
    default: ''
runs:
  using: 'docker'
  image: 'Dockerfile'